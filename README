funcparserlib
=============

A recurisve descend parser library based on functional combinators.


Documentation
-------------

See comments inside modules `funcparserlib.parser` and `funcparserlib.lexer`.

There a couple of examples available in `./examples` directory:

* GraphViz DOT parser
* JSON paser

<!-- vim: set ft=markdown tw=80: -->

